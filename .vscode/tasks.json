{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "run debug_util",
			"type": "shell",
			"command": "cd ${workspaceFolder} && cargo run --bin cmd -- ${file}",
			"problemMatcher": [
				"$rust-panic"
			]
		}
	]
}